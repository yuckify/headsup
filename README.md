# Description
This project is a heads up display for system stats. The gui shows stats like game frame rate and gpu/cpu load and temperature.

# deps
* pip3 install GPUtil distutils py-dmidecode psutil tabulate py-cpuinfo
* python -m pip install --upgrade setuptools
